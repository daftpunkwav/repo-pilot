# Third-Party Notes

`services/graph_engine`（`rp-graph-engine` / `rp_graph`）为 RepoPilot 自有实现。设计能力面参考了 MIT 许可的 [codebase-memory-mcp](https://github.com/DeusData/codebase-memory-mcp)（无运行时依赖，不链接、不调用其二进制或 SDK）。
