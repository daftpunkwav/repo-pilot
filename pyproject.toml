[project]
name = "repopilot"
version = "2.0.0"
description = "AI-driven open source project learning platform (monorepo root)"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["services/api", "services/agent", "services/mcp"]
