[project]
name = "rp-graph"
version = "0.1.0"
description = "RepoPilot 自研图谱引擎（Python 编排层与可选 HTTP sidecar）"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.sources]
"python" = ""
