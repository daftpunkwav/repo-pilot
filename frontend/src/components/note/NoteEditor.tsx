export function NoteEditor({ projectId }: { projectId: string }) {
  return (
    <div className="border border-border rounded-lg bg-surface p-4">
      <div className="text-sm text-muted">笔记编辑器占位，后续接入 Markdown 编辑 + Scribe Agent。</div>
    </div>
  );
}
